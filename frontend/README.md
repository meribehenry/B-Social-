# B-Social V2 Frontend

This was suppose to be the frontend of B-Social V2 but was stopped because trying to use vanilla JS to build it was a big mistake not because it cannot work but it makes it very very disorganised. So I took a step back to learn React so I can build it and also would eventually refactor the backend also to follow all the software design approach that I learnt.